from django.urls import path, include
from . import views
from webdev import settings

# urlpatterns = [
#     path("", views.themes_of_course, name="themes_of_course"),
#     path('<slug:slug>', views.theme, name="theme"),
# ]